# Cycle 52 — Learning Report
## What Worked
`hidden md:block` / `md:hidden` 패턴으로 동일 데이터에 두 가지 뷰 제공.

## Key Insight
테이블은 데스크톱, 카드는 모바일 — 반응형은 레이아웃만 바꾸는 게 아니라 뷰 패러다임을 바꿔야 할 때가 있음.

## UX Research Reference
- C2 해결: 모바일 DB 테이블 → 카드 뷰 전환

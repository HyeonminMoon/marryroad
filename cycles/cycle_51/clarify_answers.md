# Cycle 51 — Clarify Answers
1. 순서: CoupleMessage → DailyStreak → ProgressMilestone → WeeklyProgress → WeeklyChallenge → SmartRecommendation → NeglectedQuests → TodaySection (8개)
2. TodaySection까지 7개 카드를 지나야 함 — 모바일에서 4-5 스크롤 필요
3. 필수: TodaySection, SmartRecommendation. 상황적: ProgressMilestone(조건부), NeglectedQuests(조건부)

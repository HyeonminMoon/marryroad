# Cycle 10 — Welcome Onboarding Flow

## /welcome page with 2 steps
1. Couple names input (warm design)
2. Wedding date input (skippable)
3. Redirect to /roadmap

## Changes to homepage
- "시작하기" button goes to /welcome (new users)
- Returning users still redirect to /roadmap

## Removes from roadmap
- CoupleSetup card no longer needed at top of roadmap (handled by onboarding)
- WeddingDatePicker still shows in DdayDashboard if date not set

# Cycle 68 — Design Spec

## Before → After

### Today 탭
**Before (7개):**
CoupleMessage → WeddingDateNudge → SmartHomeSummary → SmartRecommendation → TodaySection → NeglectedQuests → 활동인사이트(4개)

**After (4개):**
CoupleMessage → WeddingDateNudge → SmartHomeSummary → TodaySection → 활동인사이트(DailyStreak + WeeklyChallenge만)

### Stats 탭
**Before (8개):**
WeeklyTrend → CompletionForecast → QuestComparison → ActivityHeatmap → AchievementGrid → BudgetChart → BudgetAllocation → ShareCardButton → QuestVisibility → DataManagement

**After (5+2개):**
WeeklyTrend → CompletionForecast → QuestComparison → ActivityHeatmap → AchievementGrid → BudgetChart → BudgetAllocation

### Settings 페이지 추가
- QuestVisibility (퀘스트 숨기기)
- ShareCardButton (공유 카드)
- DataManagement는 이미 있음 (중복 제거만)
